// const User = require('../models/UserModel.js');



// module.exports = async (req, res) => {
//     console.log('fakeUserRoute was executed');
//     const user = new User({
//         email: 'proba@gmail.com', name: 'Proba', username: 'probaproba', color: 'red'
//     });
//     const newUser = await User.register(user, 'chicken');
//     res.json(newUser);
// };

// //this is the route for doing a GET request to the /fakeUser endpoint, in order to get a fake user, 